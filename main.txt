#include <fcntl.h>
#include "libft.h"

int main(int ac, char **av)
{
	int	a = 0;
	int	fd = 0;
	char *line;
    int i = 0;

	if (ac == 2)
	{
		fd = open(av[1], O_RDONLY);
		while (i++ < 2 && (a = get_next_line(fd, &line)) == 1)
		{
			ft_putstr(line);
            ft_putstr("\n");
		}
        ft_putstr("\n\n");
        get_n_char(fd, &line, 20);
        ft_putstr(line);
        ft_putstr("\n");
	}
	return (0);
}